fortune

# color options
alias ls="ls -G"
GREEN="$(tput setaf 10)"
CYAN="$(tput setaf 14)"

#prompt
PS1="${GREEN}[\t] [\u@\h \W]\$ ${CYAN}"

# direnv
# direnv is an environment switcher for the shell.
# It knows how to hook into bash, zsh, tcsh, fish shell and elvish to load or unload environment
# variables depending on the current directory.
# This allows project-specific environment variables without cluttering the ~/.profile file.

eval "$(direnv hook zsh)"

# opam
#test -r /Users/ccianos/.opam/opam-init/init.sh && . /Users/ccianos/.opam/opam-init/init.sh > /dev/null 2> /dev/null || true

# The next line updates PATH for the Google Cloud SDK.
#if [ -f '/Users/ccianos/google-cloud-sdk/path.bash.inc' ]; then . '/Users/ccianos/google-cloud-sdk/path.bash.inc'; fi

# The next line enables shell command completion for gcloud.
#if [ -f '/Users/ccianos/google-cloud-sdk/completion.bash.inc' ]; then . '/Users/ccianos/google-cloud-sdk/completion.bash.inc'; fi

